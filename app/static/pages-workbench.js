const scenarios = [
  {id:"matter",group:"Matter opening",name:"Approved instruction",description:"An approved instruction has complete reference data and is ready for a controlled matter-opening action.",facts:[["Approval","Approved by matter owner"],["Reference data","Office and practice mapped"],["Decision boundary","Create one matter only"]],outcome:"Matter created",detail:"The simulated control layer verifies approval and reference data, creates one synthetic matter record and retains the decision trail.",controls:["Approval evidence verified","Reference data mapped","Duplicate lookup completed"],safe:true},
  {id:"duplicate",group:"Matter opening",name:"Duplicate replay",description:"The same instruction is received again after a matter has already been created.",facts:[["Existing record","Synthetic MAT-1042 found"],["Retry risk","Duplicate creation prevented"],["Decision boundary","Reconcile, do not recreate"]],outcome:"Existing matter reconciled",detail:"The browser simulation finds the prior synthetic matter and records a reconciliation outcome rather than creating a duplicate.",controls:["Stable request identifier checked","Authoritative lookup completed","Creation skipped"],safe:true},
  {id:"reference",group:"Matter opening",name:"Unmapped office",description:"A request contains an office code that is not in the approved reference map.",facts:[["Approval","Approved"],["Reference data","Office MEL-X is unmapped"],["Decision boundary","Data owner must correct it"]],outcome:"Stopped and routed to data owner",detail:"No matter is created. The exception is retained with its accountable reference-data owner.",controls:["Reference validation failed safely","No downstream action","Owner routing recorded"],safe:false},
  {id:"timeout",group:"Matter opening",name:"Timeout after commit",description:"The integration did not receive a confirmation, so retrying blindly could create a duplicate.",facts:[["Initial response","Timeout"],["Recovery rule","Look up before retry"],["Decision boundary","Verify outcome first"]],outcome:"Recovered by lookup",detail:"The browser simulation checks the synthetic target record, finds the committed matter and closes the case without replaying creation.",controls:["Timeout classified","Post-timeout lookup completed","Blind retry prevented"],safe:true},
  {id:"access",group:"AI-enabled support",name:"Restricted access request",description:"A requester needs access to material behind an information barrier.",facts:[["Information barrier","Restriction present"],["AI use","Provider call prohibited"],["Decision boundary","Barrier owner decides"]],outcome:"Stopped and routed to barrier owner",detail:"The simulation makes zero provider calls and produces no access approval. The accountable owner receives the exception.",controls:["Minimum-data boundary applied","Provider call blocked","Barrier-owner routing recorded"],safe:false},
  {id:"addin",group:"AI-enabled support",name:"Document add-in issue",description:"A general IT problem can be diagnosed using a controlled support pattern.",facts:[["Request","Synthetic add-in error"],["Data boundary","No matter content required"],["Decision boundary","User validates the fix"]],outcome:"Guided next step prepared",detail:"The simulation produces a bounded support instruction and records the evidence used; the user remains responsible for validation.",controls:["Minimum data used","Known guidance selected","User validation required"],safe:true},
  {id:"improvement",group:"Continuous improvement",name:"Prompt regression gate",description:"A candidate support prompt is compared against the agreed synthetic test contract.",facts:[["Evaluation set","Six synthetic cases"],["Candidate result","6/6 expected outcomes"],["Decision boundary","Named owner approval required"]],outcome:"Recommendation awaiting approval",detail:"The evaluation may recommend activation but cannot activate the candidate. A named workflow owner must decide.",controls:["Regression contract run","Cost proxy recorded","Autonomous activation blocked"],safe:true}
];
let selected = scenarios[0];
const list = document.querySelector("#scenario-list");
const byId = id => document.querySelector(id);
function renderList(){list.innerHTML=scenarios.map(s=>`<button class="scenario-button" type="button" data-id="${s.id}" aria-pressed="${s.id===selected.id}"><span>${s.group}</span><strong>${s.name}</strong></button>`).join("");list.querySelectorAll("button").forEach(b=>b.addEventListener("click",()=>{selected=scenarios.find(s=>s.id===b.dataset.id);history.replaceState(null,"",`#${selected.id}`);render();}));}
function render(){byId("#scenario-area").textContent=`02 · ${selected.group}`;byId("#scenario-title").textContent=selected.name;byId("#scenario-description").textContent=selected.description;byId("#case-facts").innerHTML=selected.facts.map(([k,v])=>`<div class="fact"><small>${k}</small><strong>${v}</strong></div>`).join("");byId("#run-result").hidden=true;renderList();}
function run(){const result=byId("#run-result");result.className=`run-result${selected.safe?"":" stop"}`;result.innerHTML=`<h3>${selected.outcome}</h3><p>${selected.detail}</p><p><strong>Controls:</strong> ${selected.controls.join(" · ")}</p>`;result.hidden=false;byId("#audit-trail").innerHTML=selected.controls.map((c,i)=>`<li><strong>${String(i+1).padStart(2,"0")}</strong> · ${c}</li>`).concat([`<li><strong>Decision</strong> · ${selected.outcome}</li>`]).join("");}
const match=scenarios.find(s=>s.id===location.hash.slice(1));if(match)selected=match;byId("#run-scenario").addEventListener("click",run);window.addEventListener("hashchange",()=>{const next=scenarios.find(s=>s.id===location.hash.slice(1));if(next){selected=next;render();}});render();
